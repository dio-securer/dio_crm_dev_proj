# Vite development mock transport switch.
# Development default is true even when this file is not copied to .env.
# Set false only when a real DEV API is running on the configured Vite proxy target.
VITE_DEV_MOCK_MODE=true
